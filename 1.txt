#include <iostream>
#include <math.h>
using namespace std;
int main() 
{
    int x=4, y=4, z=4;
  //  cin >> x >> y >> z;
    if ( x < 4 && y < 4 && z < 4)
    cout << "no";
    else
    cout << "yes";
    return 0;
}